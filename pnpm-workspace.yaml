packages:
  - apps/*
  - packages/*
  - tools/*

# pnpm deploy (used by the image builds) requires injected workspace packages.
injectWorkspacePackages: true

# Only these dependencies may run install scripts.
onlyBuiltDependencies:
  - esbuild
  - sharp
