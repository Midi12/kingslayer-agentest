# Shared across every module's Dockerfile (deploy/docker/*.Dockerfile). Docker build
# contexts never need what pnpm, Turborepo, tests and editors leave on disk.
.git
.gitignore
**/node_modules
**/dist
**/coverage
**/.turbo
**/*.tsbuildinfo
**/*.log
.env
gates/evidence
docs
